name: Company Knowledge Navigator - DEV
dockerFile: Dockerfile
appPort: 
  - 3000
postCreateCommand: npm install
customizations:
  vscode:
    settings:
      "terminal.integrated.defaultProfile.linux": bash
    extensions: 
      - "esbenp.prettier-vscode"
      - "dbaeumer.vscode-eslint"
forwardPorts:
  - 8000